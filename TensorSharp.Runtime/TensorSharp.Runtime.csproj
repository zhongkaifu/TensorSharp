<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <OutputPath>bin\</OutputPath>
    <IsPackable>true</IsPackable>
    <Description>Runtime services and contracts for TensorSharp GGUF inference: parsing, tokenization, prompt rendering, sampling, and output protocols.</Description>
    <PackageTags>$(TensorSharpPackageTags);runtime;tokenizer;sampling;prompt-template</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="InferenceWeb.Tests" />
    <!-- benchmarks/RadixTreeBench drives the internal PrefixTree directly (DESIGN §13.2). -->
    <InternalsVisibleTo Include="RadixTreeBench" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="StackExchange.Redis" Version="2.8.31" />
  </ItemGroup>
</Project>
