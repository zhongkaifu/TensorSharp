# Selected paired quantized projection implementation

This exact TensorSharp-owned source is integrated. Release and performance qualification remain blocked. The selected implementation shares gate/up activation sorting and quantization, and launches only the active CUDA thread-block interval while preserving original full-weight MMQ partitions and reduction ordering. It includes strong scratch allocation failure/retry behavior. It excludes the isolated own graph cache and rejected compact row kernels.

On physical GPU 6, 36 comparisons pass with bitwise gate/up equality and the unchanged full-MoE oracle/tolerances. Thirteen scratch lifecycle steps include injected growth refusal, old-shape recovery, successful new-shape growth and graph reuse. The same full suite passes with a real CUDA allocation rejection. Compute-sanitizer memcheck reports zero errors. The separate default-fusion/default-graph replay also passes all 36 comparisons and 13 lifecycle steps; both disable flags are unset. Both rank contexts in lifecycle tests share the one physical GPU. The separate physical GPU 0/6 run passes all 16 original checkpoint-shaped TP comparisons with default fusion/graphs. Its free-memory precheck, exact binary hash and shared-load qualification are recorded; it establishes bounded two-device correctness, not trained-model performance.

The 60-pair warmed microbenchmarks show approximately 0.98–1.02 times baseline for two-rank geometry, but 1.15–1.36 times baseline for narrow strips. This performance gate remains failed. Other VM GPU campaigns were active; these are per-strip MoE timings, not quiet full-checkpoint results. All measurements and exact source/binary hashes are retained.

Local ARM CPU checkpoint-shaped tests pass all 72 original comparisons and the explicit F32 precision suite passes. The combined native library, trained checkpoint, nonaligned unsupported shapes, larger physical GPU counts and final quiet benchmarks remain outstanding. Upstream ggml is unchanged at `456172ec733a135778adcd32d00e576a58232e45`.
