<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <Nullable>annotations</Nullable>
    <Description>xUnit test suite for TensorSharp runtime, model, backend, and server behavior.</Description>
    <!-- RS0030: backend availability probes are banned in test code
         (BannedSymbols.txt) — gate with [CudaFact]/[MlxFact] instead, so a
         missing backend counts as Skipped rather than silently Passed. -->
    <WarningsAsErrors>$(WarningsAsErrors);RS0030</WarningsAsErrors>
  </PropertyGroup>
  <ItemGroup>
    <AdditionalFiles Include="BannedSymbols.txt" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.CodeAnalysis.BannedApiAnalyzers" Version="3.3.4" PrivateAssets="all" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="PdfPig" Version="0.1.8" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
  </ItemGroup>
  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\AdvUtils\AdvUtils.csproj" />
    <ProjectReference Include="..\TensorSharp.Runtime\TensorSharp.Runtime.csproj" />
    <ProjectReference Include="..\TensorSharp.AgentHost\TensorSharp.AgentHost.csproj" />
    <ProjectReference Include="..\TensorSharp.Models\TensorSharp.Models.csproj" />
    <ProjectReference Include="..\TensorSharp.Server.Host\TensorSharp.Server.Host.csproj" />
    <ProjectReference Include="..\TensorSharp.Cli\TensorSharp.Cli.csproj" />
    <ProjectReference Include="..\TensorSharp.Backends.Cuda\TensorSharp.Backends.Cuda.csproj" />
    <ProjectReference Include="..\TensorSharp.Backends.MLX\TensorSharp.Backends.MLX.csproj" />
    <ProjectReference Include="..\TensorSharp.Distributed\TensorSharp.Distributed.csproj" />
  </ItemGroup>
  <PropertyGroup>
    <MlxNativeInstallDir>$(MSBuildProjectDirectory)/../TensorSharp.Backends.MLX/Native/dist</MlxNativeInstallDir>
    <MlxNativeBinaryName Condition="$([MSBuild]::IsOSPlatform('OSX'))">libmlxc.dylib</MlxNativeBinaryName>
    <MlxNativeDependencyName Condition="$([MSBuild]::IsOSPlatform('OSX'))">libmlx.dylib</MlxNativeDependencyName>
    <MlxNativeJacclName Condition="$([MSBuild]::IsOSPlatform('OSX'))">libjaccl.dylib</MlxNativeJacclName>
    <!-- MLX's precompiled Metal kernels; mlx loads it from the directory holding
         libmlx.dylib, so it has to travel with the dylibs. See
         TensorSharp.Backends.MLX.csproj for the full rationale. -->
    <MlxNativeMetallibName Condition="$([MSBuild]::IsOSPlatform('OSX'))">mlx.metallib</MlxNativeMetallibName>
  </PropertyGroup>
  <Target Name="CopyMlxNativeBinary" AfterTargets="Build" Condition="'$(MlxNativeBinaryName)' != ''">
    <ItemGroup>
      <MlxNativeBinary Include="$(MlxNativeInstallDir)/$(MlxNativeBinaryName)" Condition="Exists('$(MlxNativeInstallDir)/$(MlxNativeBinaryName)')" />
      <MlxNativeBinary Include="$(MlxNativeInstallDir)/$(MlxNativeDependencyName)" Condition="'$(MlxNativeDependencyName)' != '' And Exists('$(MlxNativeInstallDir)/$(MlxNativeDependencyName)')" />
      <MlxNativeBinary Include="$(MlxNativeInstallDir)/$(MlxNativeJacclName)" Condition="'$(MlxNativeJacclName)' != '' And Exists('$(MlxNativeInstallDir)/$(MlxNativeJacclName)')" />
      <MlxNativeBinary Include="$(MlxNativeInstallDir)/lib/$(MlxNativeJacclName)" Condition="'$(MlxNativeJacclName)' != '' And Exists('$(MlxNativeInstallDir)/lib/$(MlxNativeJacclName)')" />
      <MlxNativeBinary Include="$(MlxNativeInstallDir)/$(MlxNativeMetallibName)" Condition="'$(MlxNativeMetallibName)' != '' And Exists('$(MlxNativeInstallDir)/$(MlxNativeMetallibName)')" />
      <MlxNativeBinary Include="$(MlxNativeInstallDir)/lib/$(MlxNativeMetallibName)" Condition="'$(MlxNativeMetallibName)' != '' And !Exists('$(MlxNativeInstallDir)/$(MlxNativeMetallibName)') And Exists('$(MlxNativeInstallDir)/lib/$(MlxNativeMetallibName)')" />
    </ItemGroup>
    <Copy SourceFiles="@(MlxNativeBinary)" DestinationFolder="$(OutputPath)" SkipUnchangedFiles="true" />
  </Target>
  <!-- The ggml native library is produced by TensorSharp.GGML.Native's CMake build
       and copied into TensorSharp.Backends.GGML's output by its own post-build
       target. That copy does not reach this project: the ProjectReference item set
       is resolved before the referenced project's post-build step runs, so a rebuilt
       dylib leaves a STALE copy here and tests then fail with
       EntryPointNotFoundException for any newly added op — or, worse, silently run
       old kernels. Copy it directly from the native build directory instead. -->
  <PropertyGroup>
    <GgmlNativeBuildDirForTests>$(MSBuildProjectDirectory)/../TensorSharp.GGML.Native/build</GgmlNativeBuildDirForTests>
    <GgmlNativeBuildDirForTests Condition="$([MSBuild]::IsOSPlatform('Windows'))">$(MSBuildProjectDirectory)/../TensorSharp.GGML.Native/build-windows</GgmlNativeBuildDirForTests>
    <GgmlNativeBinaryNameForTests Condition="$([MSBuild]::IsOSPlatform('Windows'))">GgmlOps.dll</GgmlNativeBinaryNameForTests>
    <GgmlNativeBinaryNameForTests Condition="$([MSBuild]::IsOSPlatform('OSX'))">libGgmlOps.dylib</GgmlNativeBinaryNameForTests>
    <GgmlNativeBinaryNameForTests Condition="$([MSBuild]::IsOSPlatform('Linux'))">libGgmlOps.so</GgmlNativeBinaryNameForTests>
  </PropertyGroup>
  <Target Name="CopyGgmlNativeBinaryForTests" AfterTargets="Build" Condition="'$(GgmlNativeBinaryNameForTests)' != ''">
    <ItemGroup>
      <GgmlNativeForTests Include="$(GgmlNativeBuildDirForTests)/$(GgmlNativeBinaryNameForTests)" Condition="Exists('$(GgmlNativeBuildDirForTests)/$(GgmlNativeBinaryNameForTests)')" />
      <GgmlNativeForTests Include="$(GgmlNativeBuildDirForTests)/Release/$(GgmlNativeBinaryNameForTests)" Condition="Exists('$(GgmlNativeBuildDirForTests)/Release/$(GgmlNativeBinaryNameForTests)')" />
    </ItemGroup>
    <Copy SourceFiles="@(GgmlNativeForTests)" DestinationFolder="$(OutputPath)" SkipUnchangedFiles="true" />
  </Target>
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>
</Project>
