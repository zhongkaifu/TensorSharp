{
  // Pruna 8-step v0.1: a DMD step-distillation LoRA for Qwen-Image-2.1 (text-to-image and
  // editing with up to 3 references), CFG off. The card's recommended default over 5 steps.
  //
  //   TensorSharp.Cli --config config/qwen-image-2.1.json \
  //       --lora config/lora/qwen-image-2.1-pruna-8step.json --prompt "..." --width 1024 --height 1024
  //
  // Recipe (model card, PrunaAI/Pruna-Qwen-Image-2.1): these sigmas exactly, with no shift
  // ("keep the scheduler at shift=1.0 with dynamic shifting off so the sigmas are not shifted
  // twice"), then a terminal 0; true_cfg_scale 1; strength 1. The file's PEFT metadata says
  // lora_alpha 128 at rank 64, so the applied scale is 2.0 (a loader that assumes alpha = rank
  // applies half the adapter). Trained at 1K only; other step counts are not supported.
  // License: Qwen Research License (non-commercial).
  "type": "qwen-image-2.1-lora",
  "variables": {
    "root": "${TENSORSHARP_MODELS:-../../models}/qwen-image-2.1/loras"
  },
  "weights": {
    "path": "${root}/p_qwen_image_2.1_8step_v0.1.safetensors",
    "urls": [
      "https://huggingface.co/PrunaAI/Pruna-Qwen-Image-2.1/resolve/113e63bb993001b3411eb3470b84fc444040cd7e/p_qwen_image_2.1_8step_v0.1.safetensors"
    ],
    "sha256": "f0865d68b02511a3a0ed232d9d1aa99cac3a94166f574b38bcbab1a7a297bb15"
  },
  "scale": 1.0,
  "sampling": {
    "steps": 8,
    "shift": "none",
    "cfg": 1.0,
    "sigmas": [
      1.0,
      0.93333334,
      0.85714287,
      0.7692308,
      0.6666667,
      0.54545456,
      0.4,
      0.22222222
    ]
  }
}
