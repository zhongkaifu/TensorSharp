{
  // MiniMax-H3 — joint video + 32 kHz stereo audio generation. This is the REF2VA
  // checkpoint: text-to-video plus REFERENCE conditioning, where the images, clips
  // and soundtracks you pass are an identity and appearance reference for a brand
  // new scene rather than frames the clip has to reproduce.
  //
  //   TensorSharp.Server.Host --config config/minimax-h3-ref2va.json
  //
  //   TensorSharp.Cli --config config/minimax-h3-ref2va.json \
  //     --ref-image person.png --ref-image jacket.png \
  //     --prompt "she walks through a night market, neon reflections" --output out.mp4
  //
  // FL2VA and Ref2VA are SEPARATE CHECKPOINTS, not a setting. This one refuses
  // --image/--end-image as keyframes and config/minimax-h3-fl2va.json refuses
  // references; the error names the other file. A plain --image here is taken as a
  // single reference, so a client that only knows how to attach "an image" — the Web
  // UI among them — works against this config without any extra field.
  //
  // Up to NINE references, in any mix: --ref-image (a still), --ref-video (a clip, or
  // a folder of frames), --ref-audio (a soundtrack), and --ref-video-audio to give the
  // i-th --ref-video its own sound. Each one takes its own stretch of the shared
  // timeline BEFORE the generated clip and keeps its own aspect ratio, so a reference
  // reads as material that came before the shot rather than as its first frame.
  //
  // Four networks cooperate, and naming them here is what lets them auto-download
  // into ${modelRoot} on the first run (~35.4 GB in total) and be reused after:
  //   1. the denoiser     (model)               the packed audio-video DiT   10.60 GiB
  //   2. Qwen3-VL-32B     (video-text-encoder)  the text encoder, 50 layers  16.97 GiB
  //   3. the video VAE    (video-vae)           16x spatial / 4x temporal     5.21 GB
  //   4. the audio VAE    (audio-vae)           32 kHz stereo soundtrack      0.61 GB
  // Only the denoiser differs from the FL2VA config; the other three are shared and
  // will not download a second time if you already ran that one.
  //
  // ONE FILE PAIR IS NOT AUTOMATED. The text-encoder GGUF carries no tokenizer, and
  // auto-download can only fill in options that are flags — the tokenizer is not
  // one. Put vocab.json and merges.txt next to the encoder in ${modelRoot} (or point
  // TS_VIDEO_TOKENIZER at whatever folder holds them):
  //   curl -L -o models/vocab.json https://huggingface.co/MiniMaxAI/MiniMax-H3/resolve/42ed227ee7df40d41602854ae760620d6eb651fe/processor/vocab.json
  //   curl -L -o models/merges.txt https://huggingface.co/MiniMaxAI/MiniMax-H3/resolve/42ed227ee7df40d41602854ae760620d6eb651fe/processor/merges.txt
  //
  // Steps and guidance are deliberately NOT set here: a shipped config must parse on
  // both hosts, and the two spell steps differently — --video-steps N on the server,
  // --diffusion-steps N on the CLI, and the server takes no --cfg at all. So the
  // model's own defaults apply (20 steps, guidance off). H3 is CFG-distilled: cfg
  // MUST stay 1.0, which the pipeline enforces by refusing anything higher. 4-8 steps
  // is the fast operating point, at the cost of some chromatic fringing around moving
  // subjects that is gone by ~20.
  //
  // The frame count snaps to the 17k+5 grid and fps is pinned to 24.
  "variables": {
    // Override the location with the TENSORSHARP_MODELS environment variable;
    // otherwise ../models, and relative paths in a config resolve against the
    // config file's folder (not the working directory), so that is a "models"
    // folder at the repository root. This file needs no editing on Windows,
    // Linux or macOS.
    "modelRoot": "${TENSORSHARP_MODELS:-../models}",
    "hf": "https://huggingface.co",
    // Both VAEs are mirrored by Comfy-Org, so each lists two sources: if the first
    // URL fails the next one is tried. The two mirrors serve byte-identical files,
    // so one "sha256" checks either.
    "gguf": "${hf}/unsloth/MiniMax-H3-GGUF/resolve/d629413c2e5b51b38c453668b75ca3b06ca92703",
    "comfy": "${hf}/Comfy-Org/MiniMax-H3/resolve/bf92c4091e333e69b8ca1998e0a669f15cb0832b"
  },

  // Every shipped config names a backend; --backend on the command line wins over
  // it, so `--backend ggml_metal` still runs this file on Apple silicon.
  "backend": "ggml_cuda",

  // The one entry that differs from minimax-h3-fl2va.json. The partition is read off
  // the FILE NAME, so keep "ref2va" in it if you rename or requantize.
  "model": {
    "path": "${modelRoot}/minimax_h3_ref2va_pruned-Q4_K.gguf",
    "urls": [ "${gguf}/minimax_h3_ref2va_pruned-Q4_K.gguf" ],
    "sha256": "2fa5840021cf6967843eaeefde9aaa277e540de02986d5ee3d5b0e6a7a8c9dec"
  },

  // Truncated to 50 layers with the final norm removed, and freed before the
  // denoise starts. Swap in -Q2_K_M.gguf (12.20 GiB) to pair with a smaller denoiser:
  // change the name in "path" and "urls" and set "sha256" to
  //   a8ccadccd57ef34c838ffb8a7da8368bb554721b2760274a1d3b0df63960b997
  // References go through this network's vision tower as well as through the video
  // VAE, so it is not optional here even for a text-only prompt.
  "video-text-encoder": {
    "path": "${modelRoot}/qwen3vl_32b_minimax_h3-Q4_K_M.gguf",
    "urls": [ "${gguf}/qwen3vl_32b_minimax_h3-Q4_K_M.gguf" ],
    "sha256": "11e6efe70a57ce7f4838c47bdbd1a1c4b8ce10e2b7747f1b065990b70f4b05fc"
  },

  "video-vae": {
    "path": "${modelRoot}/minimax_h3_video_vae_fp16.safetensors",
    "urls": [
      "${gguf}/vae/minimax_h3_video_vae_fp16.safetensors",
      "${comfy}/vae/minimax_h3_video_vae_fp16.safetensors"
    ],
    "sha256": "7c1f131492e7eddacaac9069a61b81bdd39de5cc96561e677c5eab1cdce5e522"
  },

  // Decodes the jointly generated audio latent to 32 kHz stereo, written as a
  // sidecar .wav beside the video. Its ENCODER is also what a --ref-audio goes
  // through, so dropping this entry costs you reference soundtracks as well as the
  // generated one.
  "audio-vae": {
    "path": "${modelRoot}/minimax_h3_audio_vae_fp32.safetensors",
    "urls": [
      "${gguf}/vae/minimax_h3_audio_vae_fp32.safetensors",
      "${comfy}/vae/minimax_h3_audio_vae_fp32.safetensors"
    ],
    "sha256": "8e505d95dd1561d47abd43d4238fd40d9bb1ae9e147ed0a4cba778d76ae4db48"
  },

  "width": 640,
  "height": 384,
  "video-frames": 22,
  "fps": 24
}
